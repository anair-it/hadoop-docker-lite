zookeeper:
  image: anoop/zookeeper:3.4.6
  ports:
    - "2181:2181"
kafka:
  build: .
  ports:
    - "9092:9092"
  links:
    - zookeeper:zk
  environment:
    KAFKA_ADVERTISED_HOST_NAME: kafka
    KAFKA_CREATE_TOPICS: "anoop:1:1"
  volumes:
    - /var/run/docker.sock:/var/run/docker.sock
